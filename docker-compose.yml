services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: orignym
      POSTGRES_PASSWORD: orignym_password
      POSTGRES_DB: orignym
    ports:
      - "16247:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: