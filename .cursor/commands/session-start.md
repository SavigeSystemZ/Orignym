Start the session by:

1. loading the canonical context
2. reading `WHERE_LEFT_OFF.md`
3. reading `TODO.md` and `FIXME.md`
4. reading `PLAN.md` and `TEST_STRATEGY.md` if implementation is likely
5. summarizing current objective, blockers, next best step, and missing context surfaces
